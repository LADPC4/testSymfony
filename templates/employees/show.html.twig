{% extends 'base.html.twig' %}

{% block title %}{{ sddEmployee.name }}{% endblock %}

{% block body %}
    <h1>{{ sddEmployee.name }}</h1>

    <table>
        <tbody>
            <tr>
                <th>Position</th>
                <td>{{ sddEmployee.position }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ sddEmployee.status }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}